{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "pnpm run build && node dist/app.media-regression.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/jwt": "^10.0.0",
    "@fastify/multipart": "^9.4.0",
    "@fastify/static": "^9.0.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.0",
    "fastify-plugin": "^5.1.0",
    "redis": "^4.7.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@fork-weibo/eslint-config": "workspace:*",
    "@fork-weibo/typescript-config": "workspace:*",
    "tsx": "^4.20.5"
  }
}
